name: ynotes_components
description: yNotes' components library.
publish_to: 'none'
version: 0.2.0+6
homepage: https://github.com/EduWireApps/ynotes-components

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sizer: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
