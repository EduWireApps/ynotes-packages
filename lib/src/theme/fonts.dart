part of theme;

/// A class that manages the fonts of a theme.
class YTFonts {
  /// The primary font. Usually used for titles and importants texts.
  final String primary;

  /// The secondary font. Usually used for any texts.
  final String secondary;

  /// A class that manages the fonts of a theme.
  YTFonts({required this.primary, required this.secondary});
}
